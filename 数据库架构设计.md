# AI 费用管理平台 MVP 数据库架构设计

> 数据库：SQLite 3.31+  
> 适用项目：AI 费用管理平台 MVP  
> 规范依据：`项目规范.md`  
> SQL 文件：`database.sql`  
> 文档版本：v1.0  
> 建立日期：2026-08-13

---

## 1. 设计原则

1. 使用 SQLite，便于本地开发和单服务器部署。
2. 所有业务主键使用应用生成的 TEXT ID，例如 `user_001`。
3. 时间字段统一使用 ISO 8601 字符串；SQLite 中存储为 `TEXT`。
4. 金额字段统一使用 `REAL`，单位为人民币元；原始币种和汇率单独保留。
5. 费用必须归属到员工和部门，项目为可选维度。
6. 部门预算采用年度部门年包。
7. 额度以部门共享额度为主。
8. 使用率预警和年度预算执行率分开计算。
9. 删除业务数据默认采用限制删除或逻辑状态，不直接物理删除关键记录。
10. 所有外键约束通过 `PRAGMA foreign_keys = ON` 开启。

---

## 2. 表清单

| 表名 | 中文名称 | 作用 | MVP |
|---|---|---|---:|
| `departments` | 部门表 | 存储公司部门 | 是 |
| `users` | 用户表 | 存储员工、主管和预算管理员 | 是 |
| `projects` | 项目表 | 存储费用统计项目 | 是 |
| `employee_projects` | 员工项目关系表 | 员工与项目多对多关联 | 是 |
| `ai_tools` | AI 工具表 | 存储 8 款 AI 工具 | 是 |
| `ai_models` | AI 模型表 | 存储工具下的模型 | 是 |
| `annual_budgets` | 年度部门预算表 | 存储部门年度年包预算 | 是 |
| `department_quotas` | 部门共享额度表 | 存储部门年度共享额度 | 是 |
| `applications` | 申请表 | 存储额度、预算和额外使用申请 | 是 |
| `usage_records` | 费用使用记录表 | 存储实际 AI 使用费用 | 是 |
| `alert_settings` | 预警配置表 | 存储使用率预警阈值 | 是 |
| `alerts` | 预警表 | 存储使用率和预算预警 | 是 |
| `audit_logs` | 操作日志表 | 记录关键业务操作 | 是 |

---

## 3. 表结构设计

### 3.1 `departments` 部门表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 部门主键 |
| `code` | `TEXT` | 是 | 无 | 部门编码，唯一 |
| `name` | `TEXT` | 是 | 无 | 部门名称，唯一 |
| `status` | `TEXT` | 是 | `'active'` | `active`、`inactive` |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 更新时间 |

### 3.2 `users` 用户表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 用户主键 |
| `username` | `TEXT` | 是 | 无 | 登录账号，唯一 |
| `password_hash` | `TEXT` | 是 | 无 | 密码哈希；Demo 可使用占位哈希 |
| `name` | `TEXT` | 是 | 无 | 用户姓名 |
| `email` | `TEXT` | 是 | 无 | 邮箱，唯一 |
| `role` | `TEXT` | 是 | 无 | `employee`、`manager`、`budget_admin` |
| `department_id` | `TEXT` | 是 | 无 | 所属部门，外键关联 `departments.id` |
| `status` | `TEXT` | 是 | `'active'` | `active`、`inactive` |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 更新时间 |

### 3.3 `projects` 项目表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 项目主键 |
| `code` | `TEXT` | 是 | 无 | 项目编码，唯一 |
| `name` | `TEXT` | 是 | 无 | 项目名称 |
| `department_id` | `TEXT` | 是 | 无 | 所属部门 |
| `manager_id` | `TEXT` | 否 | `NULL` | 项目负责人，关联 `users.id` |
| `status` | `TEXT` | 是 | `'active'` | `active`、`completed`、`archived` |
| `start_date` | `TEXT` | 否 | `NULL` | 项目开始日期 |
| `end_date` | `TEXT` | 否 | `NULL` | 项目结束日期 |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 更新时间 |

### 3.4 `employee_projects` 员工项目关系表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `employee_id` | `TEXT` | 是 | 无 | 员工 ID，关联 `users.id` |
| `project_id` | `TEXT` | 是 | 无 | 项目 ID，关联 `projects.id` |
| `project_role` | `TEXT` | 否 | `NULL` | 员工在项目中的角色 |
| `joined_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 加入项目时间 |

主键为 `(employee_id, project_id)`，防止同一员工重复加入同一项目。

### 3.5 `ai_tools` AI 工具表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 工具主键 |
| `code` | `TEXT` | 是 | 无 | 工具编码，唯一 |
| `name` | `TEXT` | 是 | 无 | 工具名称，唯一 |
| `vendor` | `TEXT` | 是 | 无 | 供应商 |
| `billing_type` | `TEXT` | 是 | 无 | `seat`、`token`、`credit`、`call`、`fixed`、`mixed` |
| `default_currency` | `TEXT` | 是 | `'CNY'` | 默认币种，`CNY` 或 `USD` |
| `status` | `TEXT` | 是 | `'active'` | `active`、`inactive` |
| `description` | `TEXT` | 否 | `NULL` | 工具说明 |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 更新时间 |

### 3.6 `ai_models` AI 模型表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 模型主键 |
| `tool_id` | `TEXT` | 是 | 无 | 所属工具 |
| `code` | `TEXT` | 是 | 无 | 模型编码；同一工具内唯一 |
| `name` | `TEXT` | 是 | 无 | 模型名称 |
| `unit` | `TEXT` | 是 | `'call'` | `token`、`credit`、`call`、`seat`、`fixed` |
| `input_price` | `REAL` | 是 | `0` | 输入单价 |
| `output_price` | `REAL` | 是 | `0` | 输出单价 |
| `currency` | `TEXT` | 是 | `'CNY'` | 价格币种 |
| `status` | `TEXT` | 是 | `'active'` | `active`、`inactive` |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 更新时间 |

### 3.7 `annual_budgets` 年度部门预算表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 预算主键 |
| `department_id` | `TEXT` | 是 | 无 | 所属部门 |
| `budget_year` | `INTEGER` | 是 | 无 | 预算年度 |
| `initial_amount` | `REAL` | 是 | `0` | 年度初始预算 |
| `increase_amount` | `REAL` | 是 | `0` | 审批通过的预算增加金额 |
| `total_amount` | `REAL` | 是 | 计算列 | `initial_amount + increase_amount` |
| `used_amount` | `REAL` | 是 | `0` | 年度已使用金额 |
| `status` | `TEXT` | 是 | `'active'` | `draft`、`active`、`closed` |
| `created_by` | `TEXT` | 是 | 无 | 创建人，关联 `users.id` |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 更新时间 |

唯一约束：`department_id + budget_year`，同一部门同一年度只能有一条年度预算。

### 3.8 `department_quotas` 部门共享额度表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 额度主键 |
| `department_id` | `TEXT` | 是 | 无 | 所属部门 |
| `quota_year` | `INTEGER` | 是 | 无 | 额度年度 |
| `allocated_amount` | `REAL` | 是 | `0` | 已分配/已批准额度 |
| `used_amount` | `REAL` | 是 | `0` | 实际已使用额度 |
| `status` | `TEXT` | 是 | `'active'` | `draft`、`active`、`closed` |
| `created_by` | `TEXT` | 是 | 无 | 创建人 |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 更新时间 |

额度剩余和额度使用率建议查询时计算：

```text
remaining_amount = allocated_amount - used_amount
usage_rate = used_amount / allocated_amount × 100%
```

### 3.9 `applications` 申请表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 申请主键 |
| `application_no` | `TEXT` | 是 | 无 | 申请编号，唯一 |
| `type` | `TEXT` | 是 | 无 | `quota`、`budget_increase`、`tool_access`、`extra_usage` |
| `applicant_id` | `TEXT` | 是 | 无 | 申请人 |
| `department_id` | `TEXT` | 是 | 无 | 申请人所属部门 |
| `project_id` | `TEXT` | 否 | `NULL` | 关联项目 |
| `tool_id` | `TEXT` | 否 | `NULL` | 申请使用的 AI 工具 |
| `budget_id` | `TEXT` | 否 | `NULL` | 关联年度预算 |
| `requested_amount` | `REAL` | 是 | 无 | 申请金额 |
| `approved_amount` | `REAL` | 否 | `NULL` | 审批通过金额 |
| `reason` | `TEXT` | 是 | 无 | 申请原因 |
| `expected_usage` | `TEXT` | 否 | `NULL` | 预计用量描述 |
| `start_date` | `TEXT` | 否 | `NULL` | 使用开始日期 |
| `end_date` | `TEXT` | 否 | `NULL` | 使用结束日期 |
| `status` | `TEXT` | 是 | `'pending'` | `draft`、`pending`、`approved`、`rejected`、`cancelled` |
| `reviewer_id` | `TEXT` | 否 | `NULL` | 审批人 |
| `reviewer_comment` | `TEXT` | 否 | `NULL` | 审批意见 |
| `submitted_at` | `TEXT` | 否 | `NULL` | 提交时间 |
| `reviewed_at` | `TEXT` | 否 | `NULL` | 审批时间 |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 更新时间 |

### 3.10 `usage_records` 费用使用记录表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 费用记录主键 |
| `employee_id` | `TEXT` | 是 | 无 | 实际使用员工 |
| `department_id` | `TEXT` | 是 | 无 | 费用归属部门 |
| `project_id` | `TEXT` | 否 | `NULL` | 费用归属项目 |
| `tool_id` | `TEXT` | 是 | 无 | AI 工具 |
| `model_id` | `TEXT` | 否 | `NULL` | AI 模型 |
| `application_id` | `TEXT` | 否 | `NULL` | 关联申请 |
| `usage_type` | `TEXT` | 是 | 无 | `seat`、`token`、`credit`、`call`、`fixed` |
| `quantity` | `REAL` | 是 | `0` | 原始用量 |
| `original_currency` | `TEXT` | 是 | `'CNY'` | 原始币种 |
| `original_amount` | `REAL` | 是 | 无 | 原始金额 |
| `exchange_rate` | `REAL` | 是 | `1` | 折算汇率 |
| `amount` | `REAL` | 是 | 无 | 折算人民币金额 |
| `source` | `TEXT` | 是 | `'manual'` | `manual`、`csv`、`supplier` |
| `status` | `TEXT` | 是 | `'confirmed'` | `pending`、`confirmed`、`disputed`、`void` |
| `occurred_at` | `TEXT` | 是 | 无 | 费用发生时间 |
| `created_by` | `TEXT` | 是 | 无 | 录入人 |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 更新时间 |

### 3.11 `alert_settings` 预警配置表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 配置主键 |
| `scope_type` | `TEXT` | 是 | 无 | `global`、`department`、`user` |
| `scope_id` | `TEXT` | 否 | `NULL` | 配置范围 ID；全局配置为空 |
| `threshold_70` | `INTEGER` | 是 | `70` | 第一档预警阈值 |
| `threshold_80` | `INTEGER` | 是 | `80` | 第二档预警阈值 |
| `threshold_90` | `INTEGER` | 是 | `90` | 第三档预警阈值 |
| `threshold_100` | `INTEGER` | 是 | `100` | 用完阈值 |
| `enabled` | `INTEGER` | 是 | `1` | SQLite 布尔值，`0/1` |
| `updated_by` | `TEXT` | 是 | 无 | 修改人 |
| `updated_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 修改时间 |

### 3.12 `alerts` 预警表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 预警主键 |
| `type` | `TEXT` | 是 | 无 | 使用率、预算执行率或数据异常类型 |
| `level` | `TEXT` | 是 | 无 | `info`、`warning`、`high`、`critical` |
| `employee_id` | `TEXT` | 否 | `NULL` | 关联员工 |
| `department_id` | `TEXT` | 否 | `NULL` | 关联部门 |
| `project_id` | `TEXT` | 否 | `NULL` | 关联项目 |
| `application_id` | `TEXT` | 否 | `NULL` | 关联申请 |
| `title` | `TEXT` | 是 | 无 | 预警标题 |
| `message` | `TEXT` | 是 | 无 | 预警内容 |
| `allocated_amount` | `REAL` | 否 | `NULL` | 已申请/分配金额 |
| `used_amount` | `REAL` | 否 | `NULL` | 已使用金额 |
| `usage_rate` | `REAL` | 否 | `NULL` | 当前使用率 |
| `status` | `TEXT` | 是 | `'unread'` | `unread`、`read`、`resolved` |
| `occurred_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 预警发生时间 |
| `read_by` | `TEXT` | 否 | `NULL` | 标记者 |
| `read_at` | `TEXT` | 否 | `NULL` | 标记时间 |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 创建时间 |

### 3.13 `audit_logs` 操作日志表

| 字段名 | 数据类型 | 必填 | 默认值 | 字段说明 |
|---|---|---:|---|---|
| `id` | `TEXT` | 是 | 无 | 日志主键 |
| `actor_id` | `TEXT` | 是 | 无 | 操作人 |
| `action` | `TEXT` | 是 | 无 | 操作动作，例如 `approve`、`create` |
| `entity_type` | `TEXT` | 是 | 无 | 业务对象类型 |
| `entity_id` | `TEXT` | 是 | 无 | 业务对象 ID |
| `before_data` | `TEXT` | 否 | `NULL` | 修改前 JSON |
| `after_data` | `TEXT` | 否 | `NULL` | 修改后 JSON |
| `created_at` | `TEXT` | 是 | `CURRENT_TIMESTAMP` | 操作时间 |

---

## 4. 表关联关系

### 4.1 一对多关系

| 主表 | 从表 | 关系说明 |
|---|---|---|
| `departments` | `users` | 一个部门有多个员工，一个员工属于一个部门 |
| `departments` | `projects` | 一个部门可以有多个项目，一个项目属于一个部门 |
| `departments` | `annual_budgets` | 一个部门可以有多个年度预算，一个年度预算属于一个部门 |
| `departments` | `department_quotas` | 一个部门可以有多个年度共享额度 |
| `departments` | `applications` | 一个部门有多个申请 |
| `departments` | `usage_records` | 一个部门有多条费用记录 |
| `departments` | `alerts` | 一个部门有多条预警 |
| `ai_tools` | `ai_models` | 一个 AI 工具有多个模型 |
| `ai_tools` | `applications` | 一个工具可以关联多个申请 |
| `ai_tools` | `usage_records` | 一个工具可以产生多条费用记录 |
| `ai_models` | `usage_records` | 一个模型可以产生多条费用记录 |
| `users` | `applications` | 一个用户可以提交多个申请 |
| `users` | `usage_records` | 一个用户可以产生多条费用记录 |
| `users` | `audit_logs` | 一个用户可以产生多条操作日志 |
| `projects` | `usage_records` | 一个项目可以有多条费用记录 |
| `projects` | `applications` | 一个项目可以关联多个申请 |
| `applications` | `usage_records` | 一个申请可以关联多条实际费用记录 |
| `applications` | `alerts` | 一个申请可以产生多条预警 |

### 4.2 多对多关系

| 两端表 | 中间表 | 关系说明 |
|---|---|---|
| `users` ↔ `projects` | `employee_projects` | 员工可以参与多个项目，一个项目可以有多个员工 |

### 4.3 一对一/唯一业务关系

数据库不设置单独的一对一物理表关系，但以下组合通过唯一约束实现业务上的一对一：

| 表 | 唯一组合 | 业务含义 |
|---|---|---|
| `annual_budgets` | `department_id + budget_year` | 一个部门一个年度只有一条年度预算 |
| `department_quotas` | `department_id + quota_year` | 一个部门一个年度只有一条共享额度 |
| `alert_settings` | `scope_type + scope_id` | 一个配置范围只有一条预警配置 |
| `ai_models` | `tool_id + code` | 同一工具下模型编码唯一 |

---

## 5. 核心查询口径

### 5.1 年度预算

```sql
SELECT
    initial_amount,
    increase_amount,
    total_amount,
    used_amount,
    total_amount - used_amount AS remaining_amount,
    CASE
        WHEN total_amount = 0 THEN 0
        ELSE ROUND(used_amount * 100.0 / total_amount, 2)
    END AS execution_rate
FROM annual_budgets;
```

### 5.2 部门共享额度使用率

```sql
SELECT
    allocated_amount,
    used_amount,
    allocated_amount - used_amount AS remaining_amount,
    CASE
        WHEN allocated_amount = 0 THEN 0
        ELSE ROUND(used_amount * 100.0 / allocated_amount, 2)
    END AS usage_rate
FROM department_quotas;
```

### 5.3 员工项目费用

```sql
SELECT
    u.name AS employee_name,
    p.name AS project_name,
    ROUND(SUM(ur.amount), 2) AS total_amount
FROM usage_records ur
JOIN users u ON u.id = ur.employee_id
LEFT JOIN projects p ON p.id = ur.project_id
WHERE ur.status = 'confirmed'
GROUP BY ur.employee_id, ur.project_id
ORDER BY total_amount DESC;
```

### 5.4 部门项目费用

```sql
SELECT
    d.name AS department_name,
    p.name AS project_name,
    ROUND(SUM(ur.amount), 2) AS total_amount
FROM usage_records ur
JOIN departments d ON d.id = ur.department_id
LEFT JOIN projects p ON p.id = ur.project_id
WHERE ur.status = 'confirmed'
GROUP BY ur.department_id, ur.project_id
ORDER BY total_amount DESC;
```

### 5.5 使用率预警

```sql
SELECT
    a.id,
    a.title,
    a.allocated_amount,
    a.used_amount,
    a.usage_rate,
    a.level,
    a.status
FROM alerts a
WHERE a.type IN ('usage_rate_70', 'usage_rate_80', 'usage_rate_90', 'usage_rate_100')
  AND a.status IN ('unread', 'read')
ORDER BY a.usage_rate DESC, a.occurred_at DESC;
```

---

## 6. Mock Data 说明

`database.sql` 已包含可直接执行的 Mock 数据，覆盖：

- 5 个部门
- 6 个用户
- 8 款 AI 工具
- 8 个 AI 模型
- 4 个项目
- 4 条员工项目关系
- 5 条年度部门预算
- 5 条部门共享额度
- 4 条申请
- 6 条费用记录
- 1 条全局预警配置
- 4 条预警
- 2 条操作日志

Demo 登录账号：

| 用户名 | 密码 | 角色 |
|---|---|---|
| `employee.demo` | `demo` | 员工 |
| `manager.demo` | `demo` | 部门主管 |
| `budget.admin` | `demo` | 预算管理员 |

说明：三类演示账号已使用 bcrypt 哈希写入 SQL；其他非登录演示用户暂不提供登录凭据，正式环境应为所有可登录用户生成独立 bcrypt 哈希。

---

## 7. 执行方式

### 7.1 使用 SQLite 命令行

```bash
sqlite3 data/ai-ems.sqlite < database.sql
```

### 7.2 使用 Node.js

后端启动时执行 `database.sql`，或使用 Drizzle migration 管理表结构。

### 7.3 验证表和数据

```sql
.tables
SELECT COUNT(*) FROM departments;
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM usage_records;
PRAGMA foreign_key_check;
```

---

## 8. 重要实现说明

1. `annual_budgets.total_amount` 使用 SQLite 生成列，避免初始预算和追加预算不一致。
2. `used_amount` 当前由费用录入/导入流程维护；后续可以增加事务或定时汇总任务。
3. `alerts.usage_rate` 保存触发预警时的快照，避免后续费用变化导致历史预警内容改变。
4. `usage_records.department_id` 即使可以从员工表推导，也保留为快照字段，方便历史部门变更后仍能正确统计。
5. `project_id` 允许为空，以兼容无法归属项目的固定订阅或公共费用。
6. 预算超出当前不通过数据库约束阻止，符合“暂由预算管理员人工控制”的 MVP 规则。
7. 后端必须在写入费用时校验员工、部门和项目的归属关系，不能仅依赖前端传参。

