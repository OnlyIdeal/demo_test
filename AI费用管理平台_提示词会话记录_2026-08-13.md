# AI 费用管理平台
## 2026 年 8 月 13 日提示词会话记录（考核归档）

> 归档日期：2026-08-13  
> 记录范围：仅保留 2026 年 8 月 13 日围绕本项目的有效提示词会话。  
> 去重规则：合并重复部署命令、重复粘贴服务器地址、重复登录/Workbench 指令和浏览器环境提示；保留会产生新需求、新决策、新实现或新测试结果的内容。

## 一、考核维度映射

| 考核维度 | 本项目对应证明 |
|---|---|
| 1. 需求分析及任务拆解 | 从业务背景提炼角色、预算、额度、项目、预警和 MVP 闭环；拆分数据库、API、前端、测试和部署任务 |
| 2. UI/UX 用户体验 | 规划角色化路由、驾驶舱、列表、详情、表单、审批页面，制作 HTML 原型并实现桌面/移动端交互 |
| 3. AI 工具及提示词使用熟练度 | 连续使用结构化提示词要求 AI 输出需求文档、项目规范、数据库、接口、前端、测试和问题清单，并根据结果迭代 |
| 4. 系统工程化能力 | 固定技术栈和目录结构，设计参数化 API、角色权限、SQLite 数据库、Mock 数据、审批状态机、构建测试和部署流程 |

## 二、需求分析及任务拆解

### 会话 01：提出业务背景和角色需求

**用户提示词要点：**

> 公司使用飞书 AI、WorkBuddy/CodeBuddy、Codex、GitHub Copilot、Trae、Claude Code、UniAPI、FastGPT，费用规则分散且计费方式不同。希望建设统一的 AI 费用管理平台。员工看个人费用和额度并申请，部门主管看部门预算和成员排名，预算管理员看公司总预算、部门对比、模型排行和预警。请共同探讨整体功能内容和系统结构。

**任务拆解：**

- 识别三类角色及数据可见范围。
- 抽取费用、额度、预算、项目、审批、预警和分析等业务域。
- 先做需求讨论，不直接进入技术实现。

**阶段产出：**

- `AI费用管理平台_需求讨论记录.md`
- 初步形成员工、部门主管、预算管理员三类角色模型。
- 形成“使用记录 → 费用归属 → 额度/预算 → 预警 → 申请审批”的核心闭环。

### 会话 02：确认预算和额度规则

**用户提示词要点：**

> 部门预算是年包预算；员工提交申请后由预算员按照部门年包核算审批；部门预算不是固定额度，可以申请增加；公司额度主要是部门共享额度；暂不自动限制超部门预算；预算要精确到员工和部门，同时支持部门/员工按项目统计；实际使用达到申请额度的 80% 等比例时触发预警。

**需求结论：**

- 年度总预算 = 初始预算 + 审批通过的预算增加金额。
- 费用记录同时归属员工、部门和项目。
- 部门共享额度是额度主模型，不强制拆成员工固定额度。
- 超预算暂由预算管理员人工控制，不做自动阻断。
- 预警使用率针对实际已使用费用，不等同于年度预算执行率。

### 会话 03：提出 MVP 约束

**用户提示词要点：**

> 要开发可直接部署服务器的 AI 费用管理平台 MVP，请梳理核心功能结构，只保留验证核心业务闭环的 P0 功能，明确“必须做”和“刻意不做”，输出用户故事、轻量技术栈建议和 Markdown 功能模块表。

**任务拆解：**

- 明确 MVP 范围和非 MVP 范围。
- 将业务闭环拆成可验收模块。
- 选择易本地配置、可直接部署的技术方案。

**阶段产出：**

- `AI费用管理平台_MVP初始设计.md`
- 明确 P0：登录和角色隔离、费用、额度、预算、项目、申请审批、预警、核心分析。
- 明确暂不做：复杂账单自动同步、精细成本优化、ROI、自动超预算阻断等。

## 三、UI/UX 用户体验

### 会话 04：规划前端页面结构

**用户提示词要点：**

> 基于 MVP 设计前端页面展示结构，梳理登录页、驾驶舱、列表、详情、表单和审批流程路由；详细描述布局；明确 API 对接和加载中、失败等状态；可以使用 `ant-design-pro-master` 组件库。

**任务拆解：**

- 按角色规划菜单和页面可见性。
- 统一桌面后台布局：左侧导航、顶部用户区、右侧内容区。
- 为列表、表单、审批和详情定义空态、加载态、错误态和成功提示。

**阶段产出：**

- 路由覆盖 `/login`、`/dashboard`、`/usage`、`/quota`、`/applications`、`/budget`、`/analytics`、`/alerts`、`/tools`、`/settings` 等核心页面。
- 使用 Ant Design 的 Card、Table、Form、Modal、Alert、Tag、Progress 等组件。

### 会话 05：制作 HTML 原型

**用户提示词要点：**

> 基于上述 MVP 功能结构，先展示可运行的 HTML 原型，用于查看整体效果。

**阶段产出：**

- `AI费用管理平台_MVP原型.html`
- `AI费用管理平台_MVP原型_预览.png`
- 原型覆盖驾驶舱、预算、费用、额度、申请审批、预警和工具管理等主要展示区域。

### 会话 06：要求实现前端核心代码

**用户提示词要点：**

> 基于页面结构和后端 API 文档编写前端核心代码，要求 API、页面、路由分离；封装统一 HTTP 请求工具；配置 Token 和错误拦截；实现核心页面和完善表单验证；给出运行命令和环境变量说明。

**阶段产出：**

- React + Vite 前端工程。
- `apps/web/src/services` 统一 API 请求层。
- Token 自动携带、统一错误处理和请求失败提示。
- React Hook Form + Zod 表单校验。
- 角色菜单隔离和受保护路由。

### 会话 07：要求审核功能完整性并继续开发

**用户提示词要点：**

> 审核“AI 费用管理平台 MVP 前端页面展示结构”，确定功能是否齐全，缺失功能继续开发。

**阶段产出：**

- `AI费用管理平台_MVP前端页面审核报告.md`
- 补齐申请详情、部门概览、年度预算、费用分析、预警中心、工具与模型、系统设置等页面。
- 明确前端页面与后端接口、权限边界和异常提示。

### 会话 08：扩展手机端版本

**用户提示词要点：**

> 当前版本可运行，需要新增手机端版本，按照现有功能执行。

**阶段产出：**

- 移动端隐藏桌面侧栏，使用底部导航。
- 增加“更多”功能抽屉，保持角色权限隔离。
- 适配手机卡片、列表横向滚动、弹窗宽度和全面屏安全区。
- 继续复用现有接口、账号、审批流和页面路由。

## 四、AI 工具及提示词使用熟练度

### 会话 09：要求创建项目规范

**用户提示词要点：**

> 先生成 `项目规范.md`，把技术栈、目录结构、接口格式写死。

**提示词方法：**

- 明确输出文件名和固定约束。
- 将技术栈、目录、接口响应、校验、权限和验收标准一次性固化。
- 后续数据库、API 和前端实现均引用该规范，减少上下文偏差。

**产出：** `项目规范.md`。

### 会话 10：要求记录对话并版本化

**用户提示词要点：**

> 建一个文档记录对话沟通过程，做版本记录；补充对话记录文档和 MVP 初始设计 Markdown 文档。

**提示词方法：**

- 将开放式沟通转换为可追踪版本。
- 区分已确认业务规则、待确认问题、版本产出和后续行动。

**产出：** `AI费用管理平台_需求讨论记录.md`、`AI费用管理平台_MVP初始设计.md`。

### 会话 11：要求反复迭代而不是只输出方案

**用户提示词要点：**

> 代码需要上传 GitHub 并部署；需要自行跑通测试，列出问题和解决方案；建立三个角色账号；如果 Mock 数据不足，直接新建数据。

**提示词方法：**

- 同时约束交付位置、运行环境、验收标准和演示账号。
- 要求发现问题后修复并记录，而不是只返回代码片段。
- 通过“开发 → 测试 → 问题清单 → 修复 → 再测试”的循环推进。

## 五、系统工程化能力

### 会话 12：设计数据库架构

**用户提示词要点：**

> 基于 MVP 设计可运行数据库架构，输出完整表结构、字段说明、关联关系、建表 SQL 和 10-20 条 Mock Data，严格按照项目规范执行。

**工程拆解：**

- 建立部门、用户、项目、工具、模型、预算、额度、申请、费用、预警和审计日志等表。
- 添加主键、外键、约束和查询索引。
- 设计员工、部门、项目三维费用归属。
- 让数据库可通过 SQL 直接初始化和演示。

**产出：** `数据库架构设计.md`、`database.sql`。

### 会话 13：设计后端 API

**用户提示词要点：**

> 基于数据库架构实现后端 RESTful API；统一返回 `{ code: 200, msg: 'success', data: {} }`；所有入参非空和类型校验；数据库使用参数化查询；提供核心代码及本地测试步骤。

**工程拆解：**

- Fastify 路由按业务模块组织。
- Zod 负责入参类型、必填和范围校验。
- Drizzle ORM 负责参数化数据库访问。
- JWT + bcrypt 实现登录认证和角色权限。
- API 统一成功、失败和异常格式。

**产出：** `apps/api/src/routes/index.ts`、`后端API接口文档.md`。

### 会话 14：打通审批状态机

**用户提示词要点：**

> 每个功能至少 10 条测试数据；审批流程打通为员工提交—主管审批—预算员审批；测试目录与界面的联动；提交成功、失败都必须提示。

**工程拆解：**

- 申请新增 `approval_stage`：`manager`、`budget_admin`、`completed`。
- 员工提交后进入主管阶段。
- 主管通过后仍为待审批，进入预算管理员阶段。
- 预算管理员通过后变为最终通过。
- 角色和审批阶段不匹配时返回 403。
- 前端只显示当前角色可执行的审批按钮。

**实测结果：**

- 主管审批返回：`已通过主管审批，等待预算管理员审批`。
- 预算员审批返回：`申请已最终通过`。
- 越级审批被拒绝。
- 申请、审批和驳回均有成功/失败提示。

### 会话 15：要求测试、提交和部署

**用户提示词要点：**

> 代码发布到 `OnlyIdeal/demo_test`，部署到 `47.94.102.218`，访问 `https://demo.onlyideal.top`；自行测试并记录问题；账号密码写在页面上并支持切换。

**工程拆解：**

- 本地执行前后端类型检查和前端生产构建。
- 登录三个角色并验证权限隔离。
- 验证申请审批完整链路。
- 提交 GitHub 并提供服务器重建命令。

**产出和状态：**

- GitHub 已推送版本：`de7bd41 feat: add mobile workspace navigation`。
- 测试问题记录在 `AI-EMS测试与部署问题清单.md`。
- 服务器重建命令已整理到部署文档；若无 SSH/Workbench 会话，需要在服务器终端执行。

## 六、去重说明

以下内容未重复记录：

- 相同的“继续部署”“直接部署”“登录 Workbench”指令合并为一次服务器发布要求。
- 相同的服务器 IP、域名、阿里云控制台链接不重复展开。
- 浏览器环境上下文、标签页数量和页面 URL 不作为业务提示词记录。
- 代码已完成后的重复确认消息不重复计为新需求。
- 8 月 18 日及之后产生的文件整理或部署追问不纳入本次 8 月 13 日会话记录。

## 七、考核验收摘要

| 验收项 | 结果 | 证明文件/代码 |
|---|---|---|
| 需求分析和任务拆解 | 已完成 | `AI费用管理平台_MVP初始设计.md`、本文第二节 |
| UI/UX 页面规划和原型 | 已完成 | `AI费用管理平台_MVP原型.html`、`apps/web/src/pages` |
| 提示词驱动的文档和代码迭代 | 已完成 | `项目规范.md`、数据库/API/前端文档 |
| 工程化实现 | 已完成 | `apps/api`、`apps/web`、`database.sql`、Docker 配置 |
| 角色和权限隔离 | 已完成 | `apps/api/src/lib/auth.ts`、`apps/web/src/lib/auth.tsx` |
| 两级审批闭环 | 已完成 | `apps/api/src/routes/index.ts`、`apps/web/src/pages/ApplicationsPage.tsx` |
| 测试和问题闭环 | 已完成 | `AI-EMS测试与部署问题清单.md`、前端审核报告 |

---

**归档结论：** 本文仅归档 2026 年 8 月 13 日的有效提示词会话，已按考核要求映射到四个能力维度，并删除无效重复信息。