# AI-EMS 测试与部署问题清单

> 日期：2026-08-13

| 编号 | 问题 | 当前状态 | 解决方案 |
|---|---|---|---|
| 1 | 本地目录开始时不是 Git 仓库 | 已解决 | 已初始化本地 `main` 分支并配置 `origin` 为 `https://github.com/OnlyIdeal/AI-EMS.git` |
| 2 | 本机没有 GitHub CLI、Git 用户身份和可用推送凭据 | 阻塞远程推送 | 提供 GitHub PAT，或在服务器/本机配置有写权限的 SSH Key；同时提供 Git 提交用户名和邮箱 |
| 3 | 本机未安装 Docker | 本地无法执行 Docker 部署验收 | 可在服务器执行 `docker compose up -d --build`，或本机安装 Docker Desktop 后复测 |
| 4 | 服务器 SSH 登录信息未提供 | 阻塞服务器部署 | 提供服务器 SSH 用户、端口、认证方式（密码或私钥）；建议使用具有 Docker 权限的部署用户 |
| 5 | 域名 DNS 和服务器 80/443 端口未现场确认 | 待确认 | 将 `demo.onlyideal.top` A 记录指向 `47.94.102.218`，放通 TCP 80/443；Caddy 会自动申请 HTTPS 证书 |
| 6 | 前端初版登录表单未触发 API | 已解决 | 改为受控表单 + Zod 提交校验，已通过浏览器端实际登录测试 |
| 7 | 年度预算接口缺少角色限制 | 已解决 | 增加 `manager`/`budget_admin` 权限守卫，员工访问返回 `403` |
| 8 | 初始 Demo 密码使用占位值 | 已解决 | 三个角色账号已改为独立 bcrypt 密码，并取消后端明文兜底 |
| 9 | 前端构建出现 JSON BOM 解析错误 | 已解决 | 将 JSON 配置统一转换为无 BOM UTF-8，生产构建已通过 |
| 10 | 前端生产构建提示 JS 包体积超过 500KB | 非阻塞 | MVP 可先接受；后续可通过路由懒加载和 Rollup 分包优化 |

## 当前角色账号

| 角色 | 账号 | 密码 |
|---|---|---|
| 员工 | `employee.demo` | `Employee@2026!` |
| 部门主管 | `manager.demo` | `Manager@2026!` |
| 预算管理员 | `budget.admin` | `BudgetAdmin@2026!` |

## 已完成测试

- API TypeScript 类型检查通过。
- 前端 TypeScript 类型检查通过。
- 前端生产构建通过。
- SQLite 表结构、外键和 Mock 数据校验通过。
- 三类角色密码登录成功。
- 员工访问年度预算接口返回 `403`。
- 预算管理员可以访问 5 条年度预算数据。
- 前端 Vite 开发服务返回 `200`。
- 浏览器实际完成员工和预算管理员登录，菜单按角色隔离。
