# AI 费用管理平台
## 真实数据权限隔离与 1000 条数据测试结果

> 测试日期：2026-08-20  
> 测试环境：本地 API `http://127.0.0.1:3000`，SQLite 初始化数据库  
> 测试账号：`employee.demo`、`manager.demo`、`budget.admin`  
> 测试结论：通过

## 1. 测试目标

验证真实系统中的数据权限边界：

```text
员工：只能查看自己的费用、申请和预警；可以查看所属部门共享额度
部门主管：只能查看和审批本部门数据
预算管理员：可以查看全公司的预算、额度、费用、申请、预警和项目
```

同时验证数据库初始化后包含 1000 条新增流水，接口能够正常分页、统计和按角色过滤。

## 2. 数据初始化结果

使用 `database.sql` 初始化后，通过 SQLite 查询得到：

| 数据表 | 数量 | 说明 |
|---|---:|---|
| departments | 10 | 10 个部门 |
| users | 19 | 员工、主管、预算管理员 |
| projects | 10 | 项目台账 |
| ai_tools | 10 | 8 款业务工具 + 2 款演示接入 |
| ai_models | 12 | 模型台账 |
| annual_budgets | 10 | 部门年度预算 |
| department_quotas | 10 | 部门共享额度 |
| applications | 10 | 申请与审批演示数据 |
| usage_records | 1010 | 原有 10 条 + 新增 1000 条 |
| alerts | 10 | 使用率和预算预警 |
| audit_logs | 10 | 审计日志 |

新增流水 ID 范围：

```text
usage_bulk_0001 ~ usage_bulk_1000
```

新增数据使用 SQLite `WITH RECURSIVE` 生成，采用 `INSERT OR IGNORE`，可重复初始化且不会重复插入。

## 3. 权限规则实现

### 3.1 员工

- `/usages`：强制使用当前登录用户 `employeeId` 过滤。
- `/applications`：强制只返回当前用户提交的申请。
- `/applications/:id`：只能查看本人申请，其他申请返回 `403`。
- `/alerts`：强制只返回当前用户的预警。
- `/quotas/overview`：只返回本人所在部门共享额度。
- `/projects`：只返回本人所在部门项目。
- `/budgets/annual`：不允许访问，返回 `403`。
- `/dashboard/overview`：不返回部门年度预算，只返回部门共享额度统计。

### 3.2 部门主管

- `/usages`：强制使用当前登录用户部门过滤，忽略跨部门查询参数。
- `/applications`：只返回当前登录用户部门的申请。
- `/applications/:id`：只能查看本部门申请。
- 审批接口：只能审批本部门申请，且必须处于主管审批阶段。
- `/alerts`：只返回本部门预警。
- `/budgets/annual`：只返回本部门年度预算。
- `/quotas/overview`：只返回本部门共享额度。
- `/projects`：只返回本部门项目。

### 3.3 预算管理员

- 预算、额度、费用、申请、预警和项目接口默认允许全公司范围。
- 支持通过部门和员工参数进行缩小范围查询。
- 可以执行最终预算审批。
- 只能审批处于 `budget_admin` 阶段的申请。

## 4. 实际接口测试结果

### 4.1 费用数据隔离

测试接口：

```http
GET /api/v1/usages?page=1&pageSize=100
```

| 角色 | HTTP | 返回总数 | 返回部门范围 | 结果 |
|---|---:|---:|---|---|
| 员工 `employee.demo` | 200 | 204 | 仅 `dept_001` | 通过 |
| 主管 `manager.demo` | 200 | 204 | 仅 `dept_001` | 通过 |
| 预算管理员 `budget.admin` | 200 | 1010 | 全公司数据 | 通过 |

员工请求中额外带入其他部门筛选参数时，后端仍以 Token 中的员工 ID 为准，不会返回其他部门数据。

主管请求中额外带入其他员工筛选参数时，后端仍以 Token 中的部门 ID 为准，不会越权查询其他部门。

### 4.2 申请数据隔离

测试接口：

```http
GET /api/v1/applications?page=1&pageSize=100
```

| 角色 | HTTP | 返回数量 | 返回部门范围 | 结果 |
|---|---:|---:|---|---|
| 员工 `employee.demo` | 200 | 2 | 仅本人申请，均为 `dept_001` | 通过 |
| 主管 `manager.demo` | 200 | 2 | 仅 `dept_001` | 通过 |
| 预算管理员 `budget.admin` | 200 | 10 | `dept_001` 至 `dept_006` | 通过 |

### 4.3 年度预算隔离

测试接口：

```http
GET /api/v1/budgets/annual?year=2026
```

| 角色 | HTTP | 返回数量 | 实际结果 |
|---|---:|---:|---|
| 员工 `employee.demo` | 403 | 0 | 禁止访问年度部门预算 |
| 主管 `manager.demo` | 200 | 1 | 仅返回 `dept_001` 年度预算 |
| 预算管理员 `budget.admin` | 200 | 10 | 返回全公司 10 个部门预算 |

### 4.4 额度、项目和驾驶舱隔离

| 接口 | 员工 | 主管 | 预算管理员 | 结果 |
|---|---:|---:|---:|---|
| `/quotas/overview?year=2026` 返回数量 | 1 | 1 | 10 | 通过 |
| `/projects` 返回数量 | 1 | 1 | 10 | 通过 |
| `/dashboard/overview` 年度预算总额 | 0 | 900000 | 3750000 | 通过 |
| `/dashboard/overview` 共享额度 | `dept_001` | `dept_001` | 全公司 | 通过 |

员工驾驶舱的年度预算字段为 0，避免把部门年度预算泄露给员工；员工仍可以看到所属部门共享额度。

### 4.5 越权详情访问

| 测试场景 | 实际 HTTP | 结果 |
|---|---:|---|
| 员工查看其他员工申请 `app_002` | 403 | 通过 |
| 主管查看其他部门申请 `app_002` | 403 | 通过 |
| 员工查看其他部门预警 `alert_002` | 404 | 通过，避免泄露资源存在性 |

## 5. 安全实现说明

- 权限范围取自登录后签发的 JWT 身份信息，不信任前端传入的部门和员工参数。
- 查询条件在 SQL 查询执行前拼接到 Drizzle ORM 条件中。
- 详情接口先查数据，再根据角色和部门/员工归属返回 403 或 404。
- 审批接口同时校验角色、部门归属、申请状态和审批阶段。
- 费用接口只有预算管理员可以新增流水，且会校验员工和部门归属一致。

## 6. 修复内容

本次发现员工驾驶舱原先可能读取部门年度预算，已调整为：

```text
员工：年度预算统计返回空范围，避免部门预算泄露
主管：只统计当前部门
预算管理员：统计全公司
```

同时将新增 1000 条流水改为可重复执行的 SQL 生成逻辑，避免手工维护大量重复 INSERT。

## 7. 最终结论

本次测试全部通过：

- 1000 条新增流水已成功初始化。
- 员工、部门主管、预算管理员的数据范围已隔离。
- 员工无法读取年度部门预算和其他员工数据。
- 部门主管无法读取其他部门数据。
- 预算管理员可以查看全公司数据。
- 越权详情访问按预期返回 `403` 或 `404`。
- 后端 TypeScript 检查通过。
- SQL 内存数据库初始化校验通过。