# AI 费用管理平台 MVP 初始设计

> 文档版本：v1.0  
> 建立日期：2026-08-13  
> 文档状态：MVP 初始设计

---

## 1. MVP 目标

AI 费用管理平台 MVP 不追求一次性覆盖所有 AI 工具、供应商、财务和采购能力，而是优先验证一条最小可运行的核心业务闭环：

> 员工查看费用/额度并提交申请 → 主管或预算管理员审批 → 产生费用记录 → 费用归属到员工、部门和项目 → 更新部门共享额度及年度预算 → 按实际使用率触发预警。

MVP 必须能够回答以下问题：

1. 员工花了多少 AI 费用？
2. 费用属于哪个部门、哪个项目？
3. 部门年度预算还剩多少？
4. 部门共享额度还剩多少？
5. 员工或部门申请的费用使用到什么程度？
6. 哪些申请需要审批？
7. 哪些费用已经接近申请额度上限？

---

## 2. 角色范围

| 角色 | MVP 职责 |
|---|---|
| 员工 | 查看个人费用、额度使用率和项目费用；提交额度或额外使用申请 |
| 部门主管 | 查看本部门预算、额度、成员和项目费用；审批员工申请；发起预算增加申请 |
| 预算管理员 | 管理年度部门预算；查看全公司数据；审批预算增加申请；处理预警 |

MVP 暂不单独建设系统管理员角色，组织、账号和权限配置由预算管理员兼任。

---

## 3. 核心业务规则

### 3.1 年度部门预算

部门预算采用年度部门年包：

```text
年度总预算 = 年度初始预算 + 审批通过的预算增加金额
预算剩余 = 年度总预算 - 已使用金额
预算执行率 = 已使用金额 / 年度总预算 × 100%
```

规则：

- 部门预算不是固定不可调整额度。
- 支持发起预算增加申请。
- 预算管理员负责审核预算增加申请。
- 暂不因部门预算达到上限而自动阻断使用。
- 部门预算超出后的控制由预算管理员人工处理。

### 3.2 部门共享额度

额度以部门共享额度为主：

```text
额度剩余 = 已申请/已分配金额 - 实际已使用金额
```

规则：

- 员工费用精确归属到员工。
- 额度消耗主要体现为部门共享额度消耗。
- 暂不采用员工固定额度切分作为主模型。
- 可预留个人专项额度、临时额度和项目额度，但不列入 MVP 主流程。

### 3.3 员工、部门和项目归属

每条费用记录需要支持：

- 员工
- 部门
- 项目
- AI 工具
- 模型
- 使用时间
- 费用金额

必须支持两类项目统计：

```text
部门 → 项目 → 项目费用
员工 → 项目 → 项目费用
```

### 3.4 使用率预警

预警依据是已申请或已分配费用的实际使用率，不等同于部门年度预算执行率：

```text
使用率 = 实际已使用金额 / 已申请或已分配金额 × 100%
```

示例：

```text
申请金额：1000 元
实际使用：800 元
使用率：80%
结果：触发 80% 使用率预警
```

默认阈值：

| 阈值 | 级别 | 含义 |
|---:|---|---|
| 70% | 提示 | 开始关注费用消耗 |
| 80% | 提醒 | 费用使用较快 |
| 90% | 高风险 | 费用即将使用完 |
| 100% | 已用完 | 申请额度已达到使用上限 |

---

## 4. P0 必须做的功能

| 模块 | 必须功能 | MVP 验证目标 |
|---|---|---|
| 登录与权限 | 三类角色登录、角色识别、按角色限制数据 | 验证不同角色看到不同数据 |
| 组织人员 | 部门、员工、主管关系 | 验证费用组织归属 |
| AI 工具台账 | 维护 8 款工具和基础模型信息 | 统一费用来源名称 |
| 年度部门预算 | 初始预算、追加预算、年度总额、已用、剩余、执行率 | 验证年度年包管理 |
| 预算增加申请 | 部门主管或授权用户提交预算追加申请 | 验证预算调整流程 |
| 部门共享额度 | 已申请/分配、已用、剩余、使用率 | 验证共享额度管理 |
| 额度申请 | 员工提交额度或额外使用申请 | 验证员工申请入口 |
| 审批流程 | 主管审批员工申请，预算管理员审批预算增加申请 | 验证申请闭环 |
| 费用记录 | 手工新增或 CSV 批量导入费用 | 在无真实厂商 API 时完成费用录入 |
| 费用归属 | 精确到员工、部门、项目、工具、模型 | 验证多维度统计基础 |
| 个人看板 | 个人费用、额度使用率、项目费用、预警 | 员工知道自己花了多少、还能用多少 |
| 部门看板 | 部门预算、额度、成员排名、项目费用 | 主管能够管理本部门 |
| 公司看板 | 公司预算、部门对比、工具/模型排行、预警 | 预算管理员掌握全局 |
| 项目分析 | 部门项目和员工项目费用统计 | 验证项目维度管理 |
| 使用率预警 | 70%、80%、90%、100% 预警 | 验证及时提醒机制 |
| 数据导出 | 费用、预算和申请 CSV 导出 | 支持人工核对和汇报 |
| 操作日志 | 申请、审批、预算调整、费用修改日志 | 保证关键操作可追溯 |

---

## 5. P0 刻意不做的功能

| 功能 | MVP 暂不做的原因 |
|---|---|
| 真实 AI 厂商 API 对接 | 不同厂商接口差异大，先验证业务流程 |
| 自动同步供应商账单 | 先使用手工录入或 CSV 导入 |
| 自动支付和扣费 | 不属于核心费用管理闭环 |
| 超部门预算自动阻断 | 当前规则是由预算管理员人工控制 |
| 自动切换低价模型 | 属于后续成本优化能力 |
| 自动模型路由 | 需要接入实际调用链路 |
| 实时汇率同步 | MVP 使用固定汇率或人工维护汇率 |
| 发票、合同、采购管理 | 属于财务和采购扩展功能 |
| AI 使用 ROI 分析 | 需要采集业务产出和收益数据 |
| 复杂多级审批流 | MVP 使用固定审批关系 |
| 复杂跨部门预算分摊 | 先验证单部门和项目归属 |
| 移动端 App | 优先完成桌面端后台 |
| 多租户管理 | 当前只服务单一公司 |
| 复杂报表设计器 | 先提供固定报表和 CSV 导出 |

---

## 6. MVP 用户故事

### 6.1 员工

| 编号 | 用户故事 | 验收结果 |
|---|---|---|
| US-E01 | As an 员工, I want to 查看本月和本年度各 AI 工具费用, So that 我知道自己花了多少。 | 可按工具、模型和时间查看个人费用 |
| US-E02 | As an 员工, I want to 查看费用所属项目, So that 我知道不同项目消耗了多少费用。 | 可查看个人项目费用 |
| US-E03 | As an 员工, I want to 查看部门共享额度, So that 我知道部门还剩多少额度。 | 可查看部门额度总额、已用和剩余 |
| US-E04 | As an 员工, I want to 查看当前申请费用使用率, So that 我能在额度快用完前申请追加。 | 70%/80%/90%/100% 显示预警 |
| US-E05 | As an 员工, I want to 提交额度或额外使用申请, So that 我可以继续使用 AI 工具。 | 可填写金额、原因、项目并提交 |
| US-E06 | As an 员工, I want to 查看申请审批进度, So that 我知道申请是否通过。 | 可查看待审批、通过、驳回状态 |

### 6.2 部门主管

| 编号 | 用户故事 | 验收结果 |
|---|---|---|
| US-M01 | As a 部门主管, I want to 查看本部门年度预算, So that 我知道部门预算执行情况。 | 可查看总额、已用、剩余和执行率 |
| US-M02 | As a 部门主管, I want to 查看部门共享额度, So that 我可以了解部门当前可用额度。 | 可查看额度使用率 |
| US-M03 | As a 部门主管, I want to 查看成员费用排名, So that 我可以识别高费用成员。 | 可按成员费用排序 |
| US-M04 | As a 部门主管, I want to 按项目查看部门费用, So that 我可以分析项目 AI 成本。 | 可查看部门项目费用排行 |
| US-M05 | As a 部门主管, I want to 审批员工额度申请, So that 我可以控制共享额度使用。 | 可通过或驳回申请并填写意见 |
| US-M06 | As a 部门主管, I want to 发起预算增加申请, So that 预算不足时可以申请追加。 | 可提交年度预算追加申请 |

### 6.3 预算管理员

| 编号 | 用户故事 | 验收结果 |
|---|---|---|
| US-B01 | As a 预算管理员, I want to 查看公司年度总预算, So that 我可以掌握全公司预算执行情况。 | 可查看公司预算汇总 |
| US-B02 | As a 预算管理员, I want to 对比各部门预算使用率, So that 我可以发现异常部门。 | 可查看部门对比和排名 |
| US-B03 | As a 预算管理员, I want to 维护部门年度预算, So that 我可以管理各部门年包预算。 | 可新增、调整和查看变更记录 |
| US-B04 | As a 预算管理员, I want to 审批部门预算增加申请, So that 我可以人工控制预算追加。 | 可审批或驳回预算追加 |
| US-B05 | As a 预算管理员, I want to 查看员工、部门和项目费用, So that 我可以核对费用归属。 | 支持多维度筛选 |
| US-B06 | As a 预算管理员, I want to 查看工具和模型费用排行, So that 我可以识别主要费用来源。 | 可按工具和模型排序 |
| US-B07 | As a 预算管理员, I want to 查看高使用率预警, So that 我可以及时人工干预。 | 可按等级、部门和时间筛选 |

---

## 7. MVP 页面范围

| 页面 | 员工 | 部门主管 | 预算管理员 |
|---|---:|---:|---:|
| 登录页 | ✓ | ✓ | ✓ |
| 工作台 | ✓ | ✓ | ✓ |
| 我的费用 | ✓ | ✓ | ✓ |
| 额度中心 | ✓ | ✓ | ✓ |
| 申请与审批 | ✓ | ✓ | ✓ |
| 部门管理 | - | ✓ | ✓ |
| 年度预算 | - | ✓ | ✓ |
| 费用分析 | 个人 | 部门 | 公司 |
| 预警中心 | 个人 | 部门 | 全公司 |
| 工具与模型 | - | - | ✓ |
| 数据导入 | - | - | ✓ |

---

## 8. 推荐技术栈

### 8.1 推荐方案：Ant Design Pro + Node.js + SQLite

| 层级 | 技术 | 选择理由 |
|---|---|---|
| 前端 | React + Umi + Ant Design Pro | 可直接复用现有组件库，适合后台系统 |
| UI | Ant Design + ProComponents | 快速实现表格、表单、审批和看板 |
| 图表 | Ant Design Charts 或 Recharts | 满足趋势、排行和部门对比 |
| 后端 | Node.js + Fastify | 轻量、启动快、前后端语言统一 |
| 数据库 | SQLite | 无需独立数据库服务，部署简单 |
| ORM | Drizzle ORM | 类型安全、迁移简单、适合 SQLite |
| 参数校验 | Zod | 前后端共享数据校验规则 |
| 认证 | JWT + bcrypt | 满足 MVP 登录和角色权限 |
| 文件导入 | Papa Parse | 支持 CSV 费用批量导入 |
| 部署 | Docker Compose + Caddy/Nginx | 适合服务器一键部署 |
| 代码仓库 | `OnlyIdeal/AI-EMS` | 统一管理代码和部署版本 |

### 8.2 选择 SQLite 的原因

- 不需要单独安装数据库服务。
- 本地开发和服务器环境一致。
- 数据库备份只需备份一个文件。
- MVP 数据量和并发量通常可以满足。
- 后续可以迁移到 MySQL 或 PostgreSQL。

### 8.3 什么时候升级数据库

出现以下情况时再考虑 MySQL 或 PostgreSQL：

- 需要多实例部署。
- 并发写入量显著增加。
- 大量真实账单自动同步。
- 历史费用数据规模明显增加。
- 需要数据库高可用和集群能力。

---

## 9. MVP 推荐项目结构

```text
ai-ems/
├── apps/
│   ├── web/                  React + Ant Design Pro 前端
│   └── api/                  Fastify 后端
├── packages/
│   ├── shared/               前后端共享类型和 Schema
│   └── config/               ESLint、TypeScript、Prettier 配置
├── data/
│   └── ai-ems.sqlite         SQLite 数据库文件
├── docker-compose.yml
├── Dockerfile
├── .env.example
├── pnpm-workspace.yaml
├── package.json
└── README.md
```

---

## 10. MVP 核心接口范围

| 模块 | 接口 |
|---|---|
| 认证 | `POST /api/v1/auth/login`、`GET /api/v1/auth/me` |
| 工作台 | `GET /api/v1/dashboard/overview`、`GET /api/v1/dashboard/trend` |
| 费用 | `GET /api/v1/usages`、`POST /api/v1/usages`、`GET /api/v1/usages/summary` |
| 预算 | `GET /api/v1/budgets/annual`、`POST /api/v1/budgets/increase-requests` |
| 额度 | `GET /api/v1/quotas/overview`、`GET /api/v1/quotas/transactions` |
| 申请 | `GET /api/v1/applications`、`POST /api/v1/applications` |
| 审批 | `POST /api/v1/applications/:id/approve`、`POST /api/v1/applications/:id/reject` |
| 项目 | `GET /api/v1/projects`、`POST /api/v1/projects` |
| 预警 | `GET /api/v1/alerts`、`PATCH /api/v1/alerts/:id/read` |
| 导出 | `GET /api/v1/usages/export`、`GET /api/v1/budgets/export` |

接口统一使用以下响应结构：

```json
{
  "success": true,
  "code": "OK",
  "message": "查询成功",
  "data": {},
  "requestId": "req_xxx",
  "timestamp": "2026-08-13T10:30:00+08:00"
}
```

---

## 11. MVP 开发顺序

1. 初始化 GitHub 仓库和前后端工程。
2. 建立用户、部门、项目、预算、额度、费用、申请和预警数据表。
3. 完成登录和角色权限。
4. 完成预算管理员维护年度部门预算。
5. 完成部门共享额度。
6. 完成员工费用和额度查看。
7. 完成额度/预算申请和审批。
8. 完成费用手工录入和 CSV 导入。
9. 完成员工、部门和项目统计。
10. 完成使用率预警。
11. 完成 CSV 导出。
12. 使用 Docker Compose 部署到服务器。
13. 上传代码到 `OnlyIdeal/AI-EMS`。
14. 完成服务器部署和 MVP 验收。

---

## 12. MVP 验收标准

### 12.1 核心流程

以下流程必须能够完整跑通：

> 新增部门年度预算 → 员工提交额度申请 → 主管审批 → 录入员工费用 → 扣减部门共享额度 → 按员工/部门/项目查看统计 → 使用率达到阈值 → 触发预警 → 预算管理员跟进。

### 12.2 功能验收

- 三类角色可以登录。
- 员工只能查看本人费用和申请。
- 部门主管可以查看本部门数据并审批员工申请。
- 预算管理员可以查看全公司数据。
- 可以维护年度部门预算。
- 可以提交和审批预算增加申请。
- 可以维护部门共享额度。
- 可以录入或导入费用。
- 费用可以归属到员工、部门和项目。
- 可以按部门项目和员工项目查看费用。
- 使用率达到预设阈值时产生预警。
- 可以导出费用、预算和申请记录。
- 关键操作能够追溯。

### 12.3 部署验收

- 本地执行 `pnpm install` 后可以启动。
- 本地执行 `pnpm dev` 可以访问前端页面。
- 服务器可以通过 Docker Compose 启动。
- SQLite 数据文件可以持久化保存。
- 服务重启后业务数据不丢失。
- GitHub 仓库 `OnlyIdeal/AI-EMS` 中包含完整项目代码和部署说明。

---

## 13. 设计版本记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-08-13 | 基于前期需求讨论，形成 AI 费用管理平台 MVP 初始设计。 |
| v1.1 | 待定 | 根据审批、项目和费用录入规则继续细化。 |

