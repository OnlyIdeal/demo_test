import { Button, Card, Descriptions, Result, Tag, Timeline, Typography, message } from 'antd';
import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { getApplication } from '../services/applications';
import type { Application } from '../types/api';
export function ApplicationDetailPage(){const {id}=useParams();const navigate=useNavigate();const [data,setData]=useState<Application>();const [loading,setLoading]=useState(true);useEffect(()=>{if(id)getApplication(id).then(setData).catch(()=>message.error('申请详情加载失败')).finally(()=>setLoading(false))},[id]);if(!loading&&!data)return <Result status="404" title="申请不存在" extra={<Button onClick={()=>navigate('/applications')}>返回列表</Button>}/>;return <div className="page-wrap narrow-page"><Button type="link" onClick={()=>navigate('/applications')}>返回申请列表</Button><Card loading={loading} title="申请详情"><Descriptions bordered column={1}><Descriptions.Item label="申请编号">{data?.applicationNo}</Descriptions.Item><Descriptions.Item label="申请类型">{data?.type}</Descriptions.Item><Descriptions.Item label="申请人">{data?.applicantName}</Descriptions.Item><Descriptions.Item label="部门">{data?.departmentName}</Descriptions.Item><Descriptions.Item label="申请金额">¥ {data?.requestedAmount?.toLocaleString()}</Descriptions.Item><Descriptions.Item label="状态"><Tag color={data?.status==='approved'?'green':data?.status==='rejected'?'red':'blue'}>{data?.status}</Tag></Descriptions.Item><Descriptions.Item label="申请原因">{data?.reason}</Descriptions.Item></Descriptions><Typography.Title level={5} style={{marginTop:24}}>流程记录</Typography.Title><Timeline items={[{children:'申请已提交'},{children:data?.status==='pending'?'等待审批':`申请已${data?.status==='approved'?'通过':'驳回'}`}]} /></Card></div>}
