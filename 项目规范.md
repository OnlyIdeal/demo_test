# 统一 AI 费用管理平台 Demo 项目规范

> 文档版本：v1.0  
> 建立日期：2026-08-13  
> 文档状态：开发基线，后续代码必须遵守本规范

## 1. 规范目的

本文件用于固定 Demo 的技术栈、目录结构、接口格式、数据模型和开发约定。除非明确修改本文件并升级版本号，否则不得随意更换技术栈、调整接口返回结构或改变目录职责。

本 Demo 的目标是验证以下业务闭环：

> 员工/主管/预算管理员登录 → 查看费用与额度 → 按员工、部门、项目统计 → 提交/审批预算与额度申请 → 按实际使用率触发预警。

当前阶段只做可运行的前端 Demo，使用本地 Mock API 模拟后端，不接入真实 AI 厂商账单。

---

## 2. 固定技术栈

### 2.1 运行环境

| 项目 | 固定规范 |
|---|---|
| Node.js | `22.x LTS` |
| 包管理器 | `pnpm 9.x` |
| 编码 | UTF-8 |
| 操作系统 | Windows、macOS、Linux 均可运行 |
| 启动命令 | `pnpm dev` |
| 构建命令 | `pnpm build` |
| 预览命令 | `pnpm preview` |

### 2.2 前端技术

| 分类 | 固定技术 | 约束 |
|---|---|---|
| 构建工具 | Vite 7 | 不使用 CRA、Webpack 自定义配置 |
| UI 框架 | React 19 | 使用函数组件和 Hooks |
| 开发语言 | TypeScript 5.8 | 禁止新增 `.jsx`、`.js` 业务文件 |
| 路由 | React Router 7 | 所有页面必须通过路由注册 |
| 样式 | Tailwind CSS 4 | 优先使用原子类，禁止大面积内联 style |
| 组件 | shadcn/ui + Radix UI | 通用交互组件优先复用 |
| 图标 | Lucide React | 禁止直接引入其他图标库 |
| 图表 | Recharts | 费用趋势、部门对比、模型排行统一使用此库 |
| 服务端状态 | TanStack Query 5 | 所有接口请求不得直接散落在页面组件中 |
| 表单 | React Hook Form + Zod | 表单提交前必须做 Schema 校验 |
| 日期 | date-fns | 禁止手写复杂日期计算 |
| Mock 接口 | MSW 2 | 开发环境默认启用 |
| 本地持久化 | localStorage | 仅用于 Demo 数据和登录状态 |
| 测试 | Vitest + Testing Library | 重点覆盖数据转换、表单校验和核心组件 |
| 代码规范 | ESLint + Prettier | 提交前执行检查 |

### 2.3 明确不使用

- 不使用 Next.js、Nuxt、Vue、Angular。
- 不使用 Redux、MobX、Pinia 等全局状态库；服务端数据使用 TanStack Query，本地 UI 状态使用 React 状态或 Context。
- 不使用 Axios；统一使用浏览器原生 `fetch` 封装。
- 不使用真实后端、真实数据库或第三方 AI 厂商 API。
- 不在页面组件内直接写 Mock 数据。
- 不在接口层返回不统一的自由格式。

---

## 3. 产品范围与角色

### 3.1 角色固定值

```ts
export type UserRole = 'employee' | 'manager' | 'budget_admin';
```

| 角色值 | 中文名称 | 数据范围 |
|---|---|---|
| `employee` | 员工 | 仅本人费用、额度、申请和本人参与项目 |
| `manager` | 部门主管 | 本部门预算、成员、项目和审批数据 |
| `budget_admin` | 预算管理员 | 全公司预算、部门、工具、模型和预警数据 |

### 3.2 Demo 固定业务规则

1. 部门预算采用年度部门年包预算。
2. 部门预算不是绝对固定值，支持预算增加申请。
3. 额度以部门共享额度为主，不做员工固定额度切分。
4. 暂不自动限制超部门预算，由预算管理员人工控制。
5. 费用必须精确到员工和部门，同时支持按项目统计。
6. 项目统计至少支持“部门—项目”和“员工—项目”两个路径。
7. 使用预警按申请/分配费用的实际使用率触发，不等同于部门年度预算上限。
8. 默认使用率预警阈值为 `70%`、`80%`、`90%`、`100%`，阈值必须可配置。
9. 费用币种统一以人民币展示，原始币种和汇率保留在数据中。
10. Demo 预置三种角色账号，可通过登录页切换角色。

---

## 4. 页面与路由规范

### 4.1 路由表

| 路由 | 页面 | 权限 |
|---|---|---|
| `/login` | 登录页 | 未登录 |
| `/` | 工作台 | 全部角色 |
| `/my-usage` | 我的使用 | `employee`、`manager`、`budget_admin` |
| `/quota` | 额度中心 | 全部角色 |
| `/applications` | 申请与审批 | 全部角色，内容按角色过滤 |
| `/department` | 部门管理 | `manager`、`budget_admin` |
| `/budget` | 预算管理 | `manager`、`budget_admin` |
| `/analytics` | 费用分析 | 全部角色，内容按角色过滤 |
| `/alerts` | 预警中心 | 全部角色，内容按角色过滤 |
| `/tools` | 工具与模型 | `budget_admin` |
| `/settings` | 系统配置 | `budget_admin` |
| `*` | 404 页面 | 全部角色 |

### 4.2 页面通用结构

所有登录后页面必须使用以下布局：

```text
AppShell
├── Sidebar              左侧导航
├── Topbar               当前用户、角色、通知、退出
└── PageContainer
    ├── Breadcrumb        面包屑
    ├── PageHeader        页面标题、说明、操作按钮
    └── PageContent       页面主体
```

页面必须具备以下状态：

- 加载中：使用 Skeleton，不显示空白页面。
- 空数据：显示说明和引导操作。
- 接口错误：显示错误提示和“重新加载”操作。
- 权限不足：显示无权限页面，不仅隐藏按钮。
- 提交成功：使用 Toast，并刷新相关查询数据。

---

## 5. 固定目录结构

```text
ai-fee-platform-demo/
├── public/
│   ├── favicon.svg
│   └── logo.svg
├── src/
│   ├── app/
│   │   ├── App.tsx                 应用入口与路由挂载
│   │   ├── AppShell.tsx            登录后整体布局
│   │   ├── router.tsx              路由定义
│   │   └── providers.tsx           Query、主题、权限等 Provider
│   ├── assets/
│   │   └── styles.css              Tailwind 入口和全局样式
│   ├── components/
│   │   ├── ui/                     shadcn/ui 基础组件
│   │   ├── layout/                 Sidebar、Topbar、PageHeader
│   │   ├── charts/                 图表通用组件
│   │   ├── data-display/           KPI 卡片、状态标签、数据表格
│   │   └── feedback/               Loading、Empty、Error、Toast
│   ├── features/
│   │   ├── auth/                   登录、当前用户、角色切换
│   │   ├── dashboard/              工作台
│   │   ├── usage/                  费用和使用记录
│   │   ├── quota/                  部门共享额度和额度使用
│   │   ├── applications/           预算/额度/工具申请与审批
│   │   ├── budget/                 年度部门预算和预算增加申请
│   │   ├── analytics/              部门、员工、项目、模型分析
│   │   ├── alerts/                 使用率预警
│   │   └── catalog/                AI 工具和模型
│   ├── hooks/                      可复用业务 Hooks
│   ├── lib/
│   │   ├── api-client.ts           fetch 封装
│   │   ├── query-client.ts         TanStack Query 配置
│   │   ├── permissions.ts          权限判断
│   │   ├── formatters.ts           金额、百分比、日期格式化
│   │   └── utils.ts                通用工具
│   ├── mocks/
│   │   ├── browser.ts              MSW 浏览器启动
│   │   ├── data.ts                 Demo 初始数据
│   │   ├── handlers.ts             Mock API 路由
│   │   └── storage.ts              localStorage 持久化
│   ├── types/
│   │   ├── api.ts                  通用接口类型
│   │   ├── auth.ts                 用户与权限类型
│   │   ├── budget.ts               预算类型
│   │   ├── usage.ts                使用和费用类型
│   │   └── application.ts          申请与审批类型
│   └── main.tsx                    React 启动入口
├── .env.example                    环境变量模板
├── .gitignore
├── eslint.config.js
├── index.html
├── package.json
├── pnpm-lock.yaml
├── postcss.config.js
├── tailwind.config.ts
├── tsconfig.json
├── vite.config.ts
└── README.md
```

### 5.1 目录职责约束

- `features/` 按业务域拆分，不按页面堆放所有代码。
- `components/` 只放跨业务复用组件，不放具体业务请求。
- `mocks/` 只模拟接口行为，页面不得直接导入 `mocks/data.ts`。
- `types/` 只定义类型和 Zod Schema，不放请求逻辑。
- `lib/api-client.ts` 是唯一的 HTTP 请求出口。
- 每个 Feature 至少包含 `components/`、`hooks/`、`api.ts`、`types.ts` 或对应文件。

推荐 Feature 内部结构：

```text
features/budget/
├── components/
│   ├── AnnualBudgetOverview.tsx
│   ├── BudgetIncreaseDialog.tsx
│   └── DepartmentBudgetTable.tsx
├── hooks/
│   └── useBudget.ts
├── api.ts
├── schemas.ts
├── types.ts
└── index.ts
```

---

## 6. 接口规范

### 6.1 基础约定

- 接口前缀固定为 `/api/v1`。
- 所有接口使用 JSON。
- 请求方法使用 REST 语义：查询 `GET`，创建 `POST`，完整修改 `PUT`，局部修改 `PATCH`。
- 删除使用 `DELETE`，Demo 中仅允许删除草稿数据。
- 时间统一使用 ISO 8601，例如 `2026-08-13T10:30:00+08:00`。
- 金额字段使用数字类型，单位为人民币元；不在金额字段中带 `¥`。
- 百分比字段使用 `0-100` 数字，例如 `80` 表示 80%。
- 分页参数统一使用 `page`、`pageSize`，默认 `page=1`、`pageSize=20`。
- 排序参数统一使用 `sortBy`、`sortOrder`，`sortOrder` 只能是 `asc` 或 `desc`。

### 6.2 成功响应格式

```json
{
  "success": true,
  "code": "OK",
  "message": "操作成功",
  "data": {},
  "requestId": "req_20260813_000001",
  "timestamp": "2026-08-13T10:30:00+08:00"
}
```

### 6.3 分页响应格式

```json
{
  "success": true,
  "code": "OK",
  "message": "查询成功",
  "data": {
    "items": [],
    "pagination": {
      "page": 1,
      "pageSize": 20,
      "total": 125,
      "totalPages": 7
    }
  },
  "requestId": "req_20260813_000002",
  "timestamp": "2026-08-13T10:30:00+08:00"
}
```

### 6.4 失败响应格式

```json
{
  "success": false,
  "code": "VALIDATION_ERROR",
  "message": "请求参数校验失败",
  "errors": [
    {
      "field": "amount",
      "message": "金额必须大于 0"
    }
  ],
  "requestId": "req_20260813_000003",
  "timestamp": "2026-08-13T10:30:00+08:00"
}
```

### 6.5 固定错误码

| 错误码 | HTTP 状态码 | 含义 |
|---|---:|---|
| `OK` | 200 | 成功 |
| `CREATED` | 201 | 创建成功 |
| `VALIDATION_ERROR` | 400 | 参数校验失败 |
| `UNAUTHORIZED` | 401 | 未登录或登录过期 |
| `FORBIDDEN` | 403 | 无权限 |
| `NOT_FOUND` | 404 | 资源不存在 |
| `CONFLICT` | 409 | 数据冲突 |
| `MOCK_ERROR` | 500 | Mock 服务异常 |

### 6.6 请求头

```text
Content-Type: application/json
Authorization: Bearer demo-token
X-Request-Id: req_xxx
```

Demo 阶段的 `demo-token` 由登录接口返回，刷新页面后保存于 localStorage。

---

## 7. 固定接口清单

### 7.1 登录与当前用户

```text
POST /api/v1/auth/login
GET  /api/v1/auth/me
POST /api/v1/auth/logout
```

登录请求：

```json
{
  "username": "employee.demo",
  "password": "Employee@2026!"
}
```

登录响应 `data`：

```json
{
  "token": "demo-token-employee",
  "user": {
    "id": "user_001",
    "name": "张小明",
    "email": "zhangxiaoming@example.com",
    "role": "employee",
    "departmentId": "dept_001",
    "departmentName": "产品研发部"
  }
}
```

### 7.2 工作台

```text
GET /api/v1/dashboard/overview
GET /api/v1/dashboard/trend?year=2026
GET /api/v1/dashboard/alerts?limit=5
```

`overview` 必须包含：年度预算、年度已用、年度剩余、预算执行率、部门共享额度、额度已用、额度剩余、待审批数量、预警数量。

### 7.3 使用和费用

```text
GET /api/v1/usages
GET /api/v1/usages/summary
GET /api/v1/usages/trend
GET /api/v1/usages/by-project
GET /api/v1/usages/by-model
```

固定过滤参数：

```text
employeeId
 departmentId
projectId
toolId
modelId
startDate
endDate
```

### 7.4 部门共享额度

```text
GET   /api/v1/quotas/overview
GET   /api/v1/quotas/transactions
POST  /api/v1/quotas/allocate
POST  /api/v1/quotas/adjust
```

额度概览必须区分：

```json
{
  "allocatedAmount": 100000,
  "usedAmount": 80000,
  "remainingAmount": 20000,
  "usageRate": 80,
  "warningLevel": "warning"
}
```

这里的 `usageRate` 表示当前已分配/申请费用的实际使用率，不表示部门年度预算执行率。

### 7.5 预算

```text
GET  /api/v1/budgets/annual
GET  /api/v1/budgets/departments
GET  /api/v1/budgets/:budgetId
POST /api/v1/budgets/increase-requests
GET  /api/v1/budgets/increase-requests
POST /api/v1/budgets/increase-requests/:requestId/approve
POST /api/v1/budgets/increase-requests/:requestId/reject
```

年度预算字段：

```json
{
  "id": "budget_001",
  "year": 2026,
  "departmentId": "dept_001",
  "initialAmount": 500000,
  "increaseAmount": 100000,
  "totalAmount": 600000,
  "usedAmount": 320000,
  "remainingAmount": 280000,
  "executionRate": 53.33,
  "status": "active"
}
```

### 7.6 申请与审批

```text
GET  /api/v1/applications
POST /api/v1/applications
GET  /api/v1/applications/:applicationId
POST /api/v1/applications/:applicationId/approve
POST /api/v1/applications/:applicationId/reject
```

申请类型固定值：

```ts
export type ApplicationType =
  | 'quota'
  | 'budget_increase'
  | 'tool_access'
  | 'extra_usage';
```

申请状态固定值：

```ts
export type ApplicationStatus =
  | 'draft'
  | 'pending'
  | 'approved'
  | 'rejected'
  | 'cancelled';
```

### 7.7 预警

```text
GET   /api/v1/alerts
PATCH /api/v1/alerts/:alertId/read
GET   /api/v1/alerts/settings
PUT   /api/v1/alerts/settings
```

预警类型固定值：

```ts
export type AlertType =
  | 'usage_rate_70'
  | 'usage_rate_80'
  | 'usage_rate_90'
  | 'usage_rate_100'
  | 'budget_execution'
  | 'data_anomaly';
```

### 7.8 工具和模型

```text
GET  /api/v1/catalog/tools
GET  /api/v1/catalog/tools/:toolId
GET  /api/v1/catalog/models
POST /api/v1/catalog/tools
PATCH /api/v1/catalog/tools/:toolId
```

---

## 8. 核心数据类型

### 8.1 使用记录

```ts
export interface UsageRecord {
  id: string;
  employeeId: string;
  employeeName: string;
  departmentId: string;
  departmentName: string;
  projectId?: string;
  projectName?: string;
  toolId: string;
  toolName: string;
  modelId?: string;
  modelName?: string;
  usageType: 'seat' | 'token' | 'credit' | 'call' | 'fixed';
  quantity: number;
  originalCurrency: 'CNY' | 'USD';
  originalAmount: number;
  exchangeRate: number;
  amount: number;
  occurredAt: string;
}
```

### 8.2 项目

```ts
export interface Project {
  id: string;
  name: string;
  code: string;
  departmentId: string;
  status: 'active' | 'completed' | 'archived';
  managerId: string;
}
```

### 8.3 预算申请

```ts
export interface BudgetIncreaseRequest {
  id: string;
  departmentId: string;
  applicantId: string;
  year: number;
  requestedAmount: number;
  reason: string;
  projectIds: string[];
  status: ApplicationStatus;
  submittedAt?: string;
  reviewedAt?: string;
  reviewerId?: string;
  reviewerComment?: string;
}
```

---

## 9. Mock 数据规范

### 9.1 固定演示账号

| 账号 | 密码 | 角色 | 部门 |
|---|---|---|---|
| `employee.demo` | `Employee@2026!` | 员工 | 产品研发部 |
| `manager.demo` | `Manager@2026!` | 部门主管 | 产品研发部 |
| `budget.admin` | `BudgetAdmin@2026!` | 预算管理员 | 财务管理中心 |

### 9.2 固定初始数据量

- 部门：5 个
- 员工：30 个
- 项目：12 个
- AI 工具：8 个
- 模型：至少 16 个
- 使用记录：至少 120 条
- 年度预算：5 条部门预算
- 申请记录：至少 12 条
- 预警记录：至少 10 条

### 9.3 本地数据行为

- 首次启动从 `mocks/data.ts` 初始化数据。
- 修改、审批、标记已读等操作写入 localStorage。
- 浏览器刷新后保留当前 Demo 数据。
- 提供“恢复演示数据”操作，清除修改并恢复初始数据。
- 不允许页面直接修改 localStorage，必须经过 Mock API。

---

## 10. UI 与交互规范

### 10.1 视觉基线

- 整体风格：企业后台、清晰、专业、克制。
- 主色：蓝色系，默认 `#2563EB`。
- 成功：绿色；提醒：橙色；危险：红色；中性：灰色。
- 页面背景使用浅灰，卡片使用白色。
- 金额统一显示两位小数；金额较大时允许使用“万元”辅助展示，但表格保留完整金额。
- 使用率显示百分比和进度条，不仅显示数字。

### 10.2 数据展示规则

- 所有看板必须同时显示“金额”和“比例”。
- 部门预算必须同时显示：年度总额、已用、剩余、执行率。
- 部门共享额度必须同时显示：已分配/申请金额、已使用、剩余、使用率。
- 员工费用明细必须支持按工具、模型、项目筛选。
- 项目分析必须可切换“按部门查看”和“按员工查看”。
- 预警必须明确区分“额度使用率预警”和“部门年度预算执行预警”。

---

## 11. 开发约束

1. 所有新增页面先在 `router.tsx` 注册，再实现页面内容。
2. 所有接口先在 `types/` 定义类型，再在 `api.ts` 封装调用。
3. 所有表单先定义 Zod Schema，再实现表单组件。
4. 所有金额、百分比、日期格式化必须调用 `lib/formatters.ts`。
5. 所有权限判断必须调用 `lib/permissions.ts`，禁止页面自行判断字符串。
6. 所有 API 加载状态、空状态和错误状态必须实现。
7. 页面组件不得直接访问 `localStorage`、MSW handler 或 Mock 数据文件。
8. 不得将业务规则硬编码在多个页面；阈值、角色和状态统一放在 `types/` 或 `lib/`。
9. 组件命名使用 PascalCase，Hooks 使用 `use` 前缀，函数和变量使用 camelCase。
10. 不使用单字母变量名，不新增无意义的 `index.tsx` 页面文件。
11. 每个功能完成后必须执行 `pnpm lint`、`pnpm test` 和 `pnpm build`。
12. 未经确认不得扩展到真实账单同步、真实支付、自动扣费或生产权限体系。

---

## 12. 环境变量

`.env.example` 固定为：

```env
VITE_API_BASE_URL=/api/v1
VITE_ENABLE_MOCK=true
VITE_DEFAULT_YEAR=2026
VITE_DEFAULT_CURRENCY=CNY
```

Demo 开发环境必须保持 `VITE_ENABLE_MOCK=true`。真实后端接入前，不得删除 Mock API。

---

## 13. 验收标准

### 13.1 可运行性

- 执行 `pnpm install` 后可以执行 `pnpm dev`。
- 浏览器打开开发地址可显示登录页。
- 三个演示账号均可登录并看到对应角色界面。
- 刷新浏览器后登录状态和 Demo 数据仍然存在。

### 13.2 业务功能

- 能查看年度部门预算、已用、剩余和执行率。
- 能查看部门共享额度、已使用、剩余和使用率。
- 能查看员工、部门、项目、工具和模型费用。
- 能在部门和员工两个视角下按项目统计。
- 能提交预算增加申请和额度申请。
- 主管和预算管理员能审批或驳回申请。
- 使用率达到阈值时能产生预警。
- 能区分使用率预警和年度预算执行预警。

### 13.3 质量要求

- 无 TypeScript 编译错误。
- 无 ESLint 错误。
- 核心页面无明显空白、溢出、遮挡或无法关闭的弹窗。
- 加载、空数据、错误、无权限状态均可展示。
- 移动端至少保证表格可横向滚动，桌面端优先适配 1440px 宽度。

---

## 14. 规范变更记录

| 版本 | 日期 | 变更内容 |
|---|---|---|
| v1.0 | 2026-08-13 | 固定 Demo 技术栈、目录结构、接口响应格式、角色、数据模型、Mock 规则和验收标准。 |



